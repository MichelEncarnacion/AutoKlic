# Optional notes if Michel later wants to migrate old Supabase data.
# Current deploy: CLEAN START — seed admin (+ optional mock cars). Do not import Supabase dumps.
#
# Optional demo inventory (photos under public/autos → /autos/*):
#   sql/seed-cars-mock.sql

This file is intentionally not part of the cPanel upload package.
