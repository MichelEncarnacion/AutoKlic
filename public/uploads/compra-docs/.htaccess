# Block direct web access to purchase documents (facturas/tenencias/verificaciones).
# Serve only via authenticated API: GET /api/upload/compra-doc?path=...
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Deny from all
</IfModule>
