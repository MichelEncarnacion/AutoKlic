# Frontend build-time env (no secrets). Copy to .env for local builds if needed.
VITE_SITE_URL=https://autoclik.michel-encarnacion.dev
VITE_API_BASE=/api

# PHP/MySQL secrets live in autoklic-config.php OUTSIDE public_html — not here.
